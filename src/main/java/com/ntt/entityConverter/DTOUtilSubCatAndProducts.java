package com.ntt.entityConverter;

import java.util.HashSet;
import java.util.List;
import java.util.Set;

import com.ntt.model.Category;
import com.ntt.model.Product;
import com.ntt.model.SubCategory;

public class DTOUtilSubCatAndProducts {
	
	/*
	 * public static SubCategoryDTO ProductBuilder(List<SubCategory> subCategory) {
	 * 
	 * Category category = new Category();
	 * 
	 * Set<SubCategory> subCategories = category.getSubCategory();
	 * Set<SubCategoryDTO> subCategoryDTOSet = new HashSet<>();
	 * 
	 * for(SubCategory subCategory1:subCategories) { SubCategoryDTO subCategoryDTO =
	 * new SubCategoryDTO(); subCategoryDTO.setSubCategoryId(((SubCategory)
	 * subCategory).getSubCategoryId());
	 * subCategoryDTO.setSubCategoryCode(((SubCategory)
	 * subCategory).getSubCategoryCode());
	 * subCategoryDTO.setSubCategoryDescription(((SubCategory)
	 * subCategory).getSubCategoryDescription());
	 * 
	 * Set<ProductDTO> productDTOSet = new HashSet<>(); Set<Product> products =
	 * ((SubCategory) subCategory).getProduct();
	 * 
	 * for(Product product: products) { ProductDTO productDTO = new ProductDTO();
	 * productDTO.setProductId(product.getProductId());
	 * productDTO.setProductCode(product.getProductCode());
	 * productDTO.setProductDescription(product.getProductDescription());
	 * productDTO.setPrice(product.getPrice());
	 * productDTO.setQuantity(product.getQuantity()); productDTOSet.add(productDTO);
	 * }
	 * 
	 * subCategoryDTO.setProductDTOSet(productDTOSet);
	 * subCategoryDTOSet.add(subCategoryDTO);
	 * 
	 * 
	 * }
	 * 
	 * return subCategoryDTO;
	 * 
	 * }
	 */
		
	}


